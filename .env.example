# APOYA Gestão — variáveis de ambiente (modo manual)
# Copie para .env.local. NUNCA commite .env.local.

# ── Supabase (obrigatório) ───────────────────────────────────────────────────
VITE_SUPABASE_URL=https://ajaqbdsalxfgrwpjbtbn.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=           # anon key — injetada no build Vite

# ── Cloudflare Worker (runtime — rotas /api/* restantes) ─────────────────────
SUPABASE_URL=https://ajaqbdsalxfgrwpjbtbn.supabase.co
SUPABASE_SERVICE_ROLE_KEY=               # SECRET — server-side only

# ── CI/CD (GitHub Secrets — não usar localmente) ─────────────────────────────
# CLOUDFLARE_API_TOKEN=
# CLOUDFLARE_ACCOUNT_ID=
# SUPABASE_ACCESS_TOKEN=                 # deploy send-invite em tag prod

# Integrações REMOVIDAS no pivô manual — não configurar até Marcio liberar:
# ASAAS_*, FOCUSNFE_*, EVOLUTION_*, PLUGGY_*, AGENTS_GATE_SECRET, MCP_*
