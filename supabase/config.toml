project_id = "ajaqbdsalxfgrwpjbtbn"

# send-invite — convite interno de usuário (auth própria: Bearer do usuário +
# checagem de role admin). Única edge function viva (sistema é manual).
[functions.send-invite]
verify_jwt = false
